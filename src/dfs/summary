Template
result = []
def backtrack(path, selecting list){
    base case: satisfied some conditions
        result.add(path)
        return

    for(XX in selectinglist){
        if(some conditions)
        backtrack(path, selecting list)
        revoke choice;
    }
}

backtracking > depth-first search